generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model StudentList {
  id    String @id @default(auto()) @map("_id") @db.ObjectId
  age   Int
  email String @unique
  name  String
  marks  Int
}
